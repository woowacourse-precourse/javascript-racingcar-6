import GameSatart from '../src/racingcar/domain/GameStart.js';

// test('테스트 설명', () => {
//   expect('검증대상').toBe('기대 결과');
// });

test('1~9까지의 숫자가 잘 나오는가', () => {
  let randomdice = GameSatart.diceThrow();
  expect(randomdice).toBe();
});
