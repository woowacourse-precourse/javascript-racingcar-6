const move = {
  min: 0,
  max: 9,
  standard: 4,
  unit: 1,
};

const name = {
  minLength: 1,
  maxLength: 5,
  separator: ",",
};

const position = {
  default: 0,
};

const CAR = {
  move,
  name,
  position,
};

export default CAR;
