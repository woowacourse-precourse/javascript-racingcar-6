const CONSTANTS = {
    maxVehicleName : 5,
    vehicleCanMove : 4,
};

const VEHICLE_MOVE = {
    move : '-',
    stop : '',
};

const MOVE_RANGE = {
    from : 0,
    to : 9
};

module.exports = { CONSTANTS, MOVE_RANGE, VEHICLE_MOVE }