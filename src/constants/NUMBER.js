const RANDOM = {
  MIN: 0,
  MAX: 9,

  THRESHOLD: 4,
};

const CAR_NAME = {
  MIN: 1,
  MAX: 5,
};

const COUNT = {
  MIN: 1,
};

Object.freeze(RANDOM, CAR_NAME, COUNT);
export default { RANDOM, CAR_NAME, COUNT };
