import { Console } from '@woowacourse/mission-utils';

const OutputView = {
  /**
   * 메시지를 출력하는 Output print 메소드
   * @param {string} message
   */
  print(message) {
    Console.print(message);
  },
};

export default OutputView;
