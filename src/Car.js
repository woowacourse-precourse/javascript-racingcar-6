class Car {
  chooseGoOrStop() {
    //0에서 9 사이에서 무작위 값을 구한 후 무작위 값이 4 이상일 경우 전진한다.
    // MissionUtils.Random.pickNumberInRange(0, 9) 활용
    // 전진이면 true, 정지면 false 불린 값을 반환한다.
  }
}

export default Car;
