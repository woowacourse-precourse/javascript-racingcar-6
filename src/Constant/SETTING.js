export const MOVESET = Object.freeze({
  stay: '',
  move: '-',
});

export const RANDOMSET = Object.freeze({
  startNum: 0,
  middleNum: 4,
  endNum: 9,
});

export const INPUTSET = Object.freeze({
  maxInputLen: 5,
  minInputLen: 1,
});
