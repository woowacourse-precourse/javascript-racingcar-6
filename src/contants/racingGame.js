export const CAR_NAME_ROLE = Object.freeze({
  minLengh: 1,
  maxLengh: 5,
});

export const ROUND_ROLE = Object.freeze({
  minRound: 1,
});

export const MOVE_ROLE = Object.freeze({
  minRange: 0,
  maxRange: 9,
  threshold: 4,
});
