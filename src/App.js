import { Console } from "@woowacourse/mission-utils";
import gameQnA from "./classes/gameQnA";
import Validator from "./classes/validator";

class App {
  async play() {
    // const carNameAnswer = await gameQnA.carsName();
    // Validator.carsNameInput(carNameAnswer);
  }
}

export default App;
