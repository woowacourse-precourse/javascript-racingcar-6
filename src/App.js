import { MissionUtils } from "@woowacourse/mission-utils";

export class Car {
  name;
  position;
  constructor(name) {
    this.name = name;
    this.position = 0;
  }
}
class App {
  async play() {}
}

export default App;