import { Console, Random } from '@woowacourse/mission-utils';

class App {
  async play() {}

  validateInput(input) {}

  getWinners(cars) {}
}

class Car {
  constructor(name) {
    this.name = name;
    this.progress = '';
  }

  moveOrStop() {}
}

export default App;
