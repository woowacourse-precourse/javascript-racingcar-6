import { MissionUtils, Console } from "@woowacourse/mission-utils";

class App {
  async play() {
    const START_GAME = async() => {
      // 게임 시작 이벤트
    }

    const RACING_CAR_GAME = async(OBJECT_CAR) => {
      // 레이싱 게임 이벤트(각 자동차의 정보를 가져오기)
    }

    const UPDATE_CAR_PROGRESS = (OBJECT_CAR) => {
      // 각 자동차의 이름과 진행도(각 자동차의 정보를 가져오기)
    }

    await START_GAME();
  }
}

export default App;
