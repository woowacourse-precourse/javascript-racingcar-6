import { Console } from "@woowacourse/mission-utils";
import MESSAGES from "../constants/Messages.js";

const askTryNumber = () => {
  Console.print(MESSAGES.ASK_TRY_NUMBER);
};

export default askTryNumber;
